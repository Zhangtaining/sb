[tool.uv.workspace]
members = [
    "shared",
    "services/ingestion",
    "services/reid",
    "services/exercise",
    "services/guidance",
    "services/api",
    "services/worker",
    "services/perception",  # torch<2.3 pinned for macOS x86_64 CPU compat
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "httpx>=0.27",
]
