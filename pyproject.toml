[tool.uv.workspace]
members = [
    "shared",
    "services/ingestion",
    "services/reid",
    "services/exercise",
    "services/guidance",
    "services/api",
    "services/worker",
    # perception is excluded from local workspace — it requires CUDA torch
    # and is only built/run via Docker. Manage it separately:
    #   cd services/perception && uv sync
    # "services/perception",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "httpx>=0.27",
]
